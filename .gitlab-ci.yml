# This folder is cached between builds
# http://docs.gitlab.com/ce/ci/yaml/README.html#cache
cache:
  paths:
  - node_modules/

test:
  script:
   # use Node Version Manager to switch to latest lts release
   - nvm use lts/erbium
   - npm install
   - npm test
